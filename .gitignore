# dependencies
node_modules/

# build output — regenerated by `npm run build`
dist/

# Uncompressed source media (100–280 MB files). Kept locally as backups only;
# GitHub rejects any single file over 100 MB, so these must never be committed.
media-originals/

# The original screen recording OneDrive is holding a lock on. The compressed
# clip.mp4 next to it is the one the site uses.
public/games/**/0909*.mp4
public/games/**/Screen_Recording*.mp4
public/games/**/TD.mp4

# editor / OS noise
.DS_Store
Thumbs.db
.vscode/
*.local
